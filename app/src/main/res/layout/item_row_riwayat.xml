<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/card_view"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_gravity="center"
    android:layout_marginStart="8dp"
    android:layout_marginTop="4dp"
    android:layout_marginEnd="8dp"
    android:layout_marginBottom="4dp"
    card_view:cardCornerRadius="4dp"
    android:background="@drawable/myactivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp"
        android:orientation="vertical">

        <!-- Transaction ID -->
        <TextView
            android:id="@+id/transactionIdTextView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="ID Transaksi: ABC123"
            android:textSize="16sp"
            android:textStyle="bold" />

        <!-- Waste Type -->
        <TextView
            android:id="@+id/wasteTypeTextView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Jenis Sampah: Plastic"
            android:textSize="16sp" />

        <!-- Partner -->
        <TextView
            android:id="@+id/partnerTextView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Mitra: Company XYZ"
            android:textSize="16sp" />

        <!-- Transaction Time -->
        <TextView
            android:id="@+id/transactionTimeTextView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Jam Transaksi: 09:00 AM"
            android:textSize="16sp" />
    </LinearLayout>
</androidx.cardview.widget.CardView>